{% extends "base.html.twig" %}

{% block body %}
	<div class="container">
		<div class="row justify-content-center">
			<div class="col-8 border border-1 border-dark bg-light shadow rounded mt-5 p-3">
				{{ form_start(form) }}
				{{ form_widget(form) }}
				<input type='submit' value='Add category' class='btn btn-success m-3'>
				{{ form_end(form) }}
			</div>
		</div>
	</div>
{% endblock %}
